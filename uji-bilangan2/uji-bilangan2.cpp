#include <iostream>
using namespace std;


int main() {
	srand(time(0));

	bilangan = rand() % 10;
	count << "nilai awalan : " << bilangan << endl;

	if (bilangan < 5)
	{
		cout << "Bilangan kurang dari 5";
		renturn 0;
	}
	bilangan = 2 * bilangan;
	cout << "nilai sekarang; " << bilangan;
}

